# 전체 플로우 테스트 가이드

## 🎯 테스트 목표
CLI → 익명 리뷰 → 회원가입 → API 키 발급 → 인증 모드 전환

---

## 📋 테스트 시나리오

### Scenario 1: 신규 사용자 (익명)

#### 1-1. 초기 상태 확인
```bash
# config.json 삭제 (깨끗한 상태)
rm ~/.claude-daily-commands/config.json

# 또는 API 키만 삭제
jq 'del(.api_key)' ~/.claude-daily-commands/config.json > /tmp/config.json && mv /tmp/config.json ~/.claude-daily-commands/config.json
```

#### 1-2. 익명 리뷰 생성
```bash
cd /path/to/your/project
/dailyreview-sync
```

**예상 결과:**
```
# 📅 Daily Review - 2025-11-12
# **3개 커밋 | 12개 파일 | +245줄 -87줄**

🔄 Own It에 업로드 중... (익명 모드)
✅ 익명 리뷰 생성 완료!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 웹에서 예쁘게 보고 싶으신가요?

브라우저에서 타임라인과 통계를 확인할 수 있습니다:
http://localhost:3000/reviews/abc-123-xyz

⚠️  주의: 익명 리뷰는 24시간 후 자동 삭제됩니다
   만료 시간: 2025-11-13 19:00:00
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

지금 브라우저에서 보시겠습니까? (Y/n)
```

#### 1-3. 브라우저 오픈
```bash
# Y 입력
Y
```

**예상 결과:**
- macOS에서 자동으로 브라우저 열림
- `http://localhost:3000/reviews/[id]` 페이지 로드
- 상단/하단에 "GitHub로 시작하기" 버튼 표시

#### 1-4. 회원가입 (브라우저)
1. "GitHub로 시작하기" 클릭
2. GitHub App 설치 페이지로 리다이렉트
3. 저장소 선택 & "Install & Authorize" 클릭
4. Own It 대시보드로 리다이렉트
5. API 키 자동 생성 팝업 표시

**Expected API Key:**
```
own_it_sk_abc123def456ghi789jkl...
```

---

### Scenario 2: 회원가입 후 API 키 설정

#### 2-1. API 키 복사 (브라우저)
대시보드에서 생성된 API 키 복사

#### 2-2. CLI 설정 실행
```bash
cd ~/development/claude-daily-commands
./scripts/setup-ownit.sh
```

**인터랙션:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔧 Own It 동기화 설정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣  Own It API URL 입력:
    (Enter = http://localhost:4000)

    > http://localhost:4000  [Enter]

2️⃣  API 키 입력:
    (own-it.dev > 설정 > API 키에서 발급)

    > own_it_sk_abc123def456ghi789jkl...  [붙여넣기]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 설정 완료!

저장 위치: ~/.claude-daily-commands/config.json

이제 다음 명령어를 사용할 수 있습니다:
  /dailyreview-sync        오늘 작업 동기화
  /dailyreview-sync week   최근 7일 동기화

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### 2-3. 설정 확인
```bash
cat ~/.claude-daily-commands/config.json
```

**예상 출력:**
```json
{
  "api_url": "http://localhost:4000",
  "api_key": "own_it_sk_abc123def456ghi789jkl..."
}
```

---

### Scenario 3: 인증된 사용자 (회원)

#### 3-1. 인증 모드 동기화
```bash
cd /path/to/your/project
/dailyreview-sync
```

**예상 결과:**
```
# 📅 Daily Review - 2025-11-12
# **3개 커밋 | 12개 파일 | +245줄 -87줄**

🔄 Own It에 동기화 중... (인증 모드)
✅ Own It 동기화 완료!
📊 대시보드: http://localhost:4000/dashboard/reviews/abc-123

## Timeline
...
```

#### 3-2. 대시보드 확인 (브라우저)
```
http://localhost:4000/dashboard
```

**확인 사항:**
- Daily 탭에 리뷰 표시
- 통계 업데이트
- 타임라인 정상 표시
- 익명 리뷰가 회원 리뷰로 이전되었는지 확인

---

### Scenario 4: 여러 날 동기화

#### 4-1. 어제 작업
```bash
/dailyreview-sync yesterday
```

#### 4-2. 최근 7일
```bash
/dailyreview-sync week
```

**예상 결과:**
- 각 날짜별 리뷰 생성
- DB에 개별 레코드 저장
- 대시보드에 리스트 표시

---

## 🐛 에러 케이스 테스트

### Case 1: API 서버 다운
```bash
# API 서버 종료
cd ~/development/own-it/apps/api
# Ctrl+C

# 동기화 시도
/dailyreview-sync
```

**예상 결과:**
```
🔄 Own It에 업로드 중... (익명 모드)
❌ 업로드 실패 (HTTP 000)

💡 로컬 리포트는 정상적으로 생성되었습니다.
```

### Case 2: 잘못된 API 키
```bash
# config.json 수정
jq '.api_key = "invalid_key"' ~/.claude-daily-commands/config.json > /tmp/config.json && mv /tmp/config.json ~/.claude-daily-commands/config.json

# 동기화 시도
/dailyreview-sync
```

**예상 결과:**
```
🔄 Own It에 동기화 중... (인증 모드)
❌ 동기화 실패: Invalid or expired API key
```

### Case 3: Git 저장소 아님
```bash
cd /tmp
/dailyreview-sync
```

**예상 결과:**
```
❌ Not a git repository
💡 Run this command in a git repository
```

### Case 4: 커밋 없음
```bash
cd /path/to/empty/repo
/dailyreview-sync
```

**예상 결과:**
```
📭 No commits found for 2025-11-12
```

---

## ✅ 검증 체크리스트

### CLI
- [ ] 익명 모드 동작 (API 키 없음)
- [ ] 인증 모드 동작 (API 키 있음)
- [ ] 브라우저 자동 오픈 (macOS)
- [ ] 회원가입 CTA 메시지 표시
- [ ] 에러 처리 (서버 다운, 잘못된 키)

### Backend
- [ ] POST /api/anonymous-reviews (201)
- [ ] GET /api/anonymous-reviews/:id (200)
- [ ] POST /api/daily-reviews/sync (200, 인증)
- [ ] API 키 검증 (401 on invalid)
- [ ] 24시간 만료 처리

### Frontend
- [ ] /reviews/[id] 페이지 렌더링
- [ ] 상단 CTA 배너 표시
- [ ] 하단 CTA 배너 표시
- [ ] 타임라인 정상 표시
- [ ] GitHub App 연동 버튼 동작
- [ ] 대시보드 리뷰 목록 표시

### 전체 플로우
- [ ] 익명 → 회원가입 → 인증 모드 전환
- [ ] 익명 리뷰 → 회원 리뷰 마이그레이션
- [ ] API 키 자동 발급
- [ ] 여러 날짜 동기화

---

## 📊 성능 테스트

### 대용량 커밋
```bash
# 100개 커밋이 있는 저장소에서
/dailyreview-sync week
```

**확인:**
- 처리 시간 < 5초
- JSON 크기 < 1MB
- API 응답 < 2초

### 동시 접속
```bash
# 여러 터미널에서 동시 실행
for i in {1..5}; do
  /dailyreview-sync &
done
```

**확인:**
- 모두 성공
- DB 충돌 없음
- 조회수 정확히 증가

---

## 🎓 사용자 매뉴얼

### 첫 사용자
```bash
# 1. CLI 설치
bash <(curl -s https://raw.githubusercontent.com/.../install.sh)

# 2. 프로젝트에서 실행
cd /your/project
/dailyreview-sync

# 3. 브라우저에서 확인 (Y 입력)

# 4. 회원가입 (GitHub App 연동)

# 5. API 키 설정
cd ~/development/claude-daily-commands
./scripts/setup-ownit.sh

# 6. 이후 자동 동기화
/dailyreview-sync
```

### 일일 사용
```bash
# 매일 작업 후
cd /your/project
/dailyreview-sync

# 또는 자동화 (cron)
0 18 * * * cd /your/project && /dailyreview-sync
```

---

**작성일**: 2025-11-12
**테스트 환경**: macOS, Node 20+, pnpm 9+
