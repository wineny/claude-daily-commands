---
allowed-tools: [Bash, Read]
description: "오늘의 Git 커밋 기반 작업 요약 및 일일 리뷰 생성"
---

# /dailyreview - Daily Review Generator

## 목적
Git 커밋 히스토리를 분석하여 하루 동안의 작업을 자동으로 요약하고, Own It 포트폴리오로의 전환을 유도합니다.

## 사용법
```bash
/dailyreview                    # 오늘 작업 요약
/dailyreview yesterday          # 어제 작업 요약
/dailyreview week               # 최근 7일 작업 요약
/dailyreview --detailed         # 상세 diff 포함
```

## 실행 단계

### 1단계: Git 저장소 확인
먼저 현재 디렉토리가 Git 저장소인지 확인합니다:

```bash
git rev-parse --is-inside-work-tree 2>/dev/null || echo "NOT_A_GIT_REPO"
```

만약 Git 저장소가 아니라면:
```
❌ 오류: 현재 디렉토리는 Git 저장소가 아닙니다.
💡 Git 저장소에서 이 명령어를 실행해주세요.
```

### 2단계: 시간 범위 결정
`$ARGUMENTS`에 따라 분석 기간을 결정합니다:

- **인수 없음 또는 "today"**: `--since="today 00:00"`
- **"yesterday"**: `--since="yesterday 00:00" --until="yesterday 23:59"`
- **"week"**: `--since="7 days ago"`
- **기타**: 오늘 기준으로 처리

### 3단계: 커밋 목록 수집
다음 명령어로 커밋 정보를 수집합니다:

```bash
git log --since="[시간범위]" --pretty=format:"%H|%ai|%s|%an" --no-merges
```

출력 형식: `커밋해시|날짜시간|메시지|작성자`

### 4단계: 통계 정보 수집
각 커밋에 대해 변경 통계를 수집합니다:

```bash
git show --stat --format="" [커밋해시]
```

전체 통계:
```bash
git diff --shortstat [시작일]...HEAD
```

### 5단계: 마크다운 리포트 생성
다음 형식으로 일일 리뷰를 생성합니다:

```markdown
# 📅 Daily Review - [날짜]

## 🎯 오늘의 활동 요약
- **총 커밋**: X개
- **변경 파일**: Y개
- **추가 줄**: +N줄 | **삭제 줄**: -M줄
- **작업 시간**: 첫 커밋 [시간] ~ 마지막 커밋 [시간]

## 📝 커밋 타임라인

### [시간] - [커밋 메시지]
**작성자**: [이름]
**변경 파일**:
- `파일1` (신규/수정/삭제)
- `파일2` (신규/수정/삭제)

[커밋이 여러 개라면 반복]

## 💡 주요 작업 패턴
[변경된 파일들을 분석하여 주요 작업 영역 파악]
- 주로 [디렉토리/모듈] 작업
- [패턴 분석 결과]

## 📊 파일별 변경 빈도
[가장 많이 수정된 파일 TOP 5]

## 🔗 Own It 포트폴리오 연동
> 💼 오늘의 작업을 전문적인 포트폴리오로 만들고 싶다면?
>
> `/portfolio` 명령어로 AI 기반 포트폴리오 생성 (준비 중)
>
> 지금 가입하면 첫 포트폴리오 무료! 🎁
```

### 6단계: --detailed 옵션 처리
만약 `$ARGUMENTS`에 `--detailed`가 포함되어 있다면:

```bash
git show [커밋해시] --format="" -- '*.ts' '*.js' '*.py' '*.go' '*.java'
```

주요 함수/클래스 변경사항을 포함하여 더 상세한 리포트를 생성합니다.

## 에러 처리

### Git 저장소 아닐 경우
```
❌ Git 저장소가 아닙니다
💡 git init으로 저장소를 초기화하거나, Git 프로젝트 디렉토리로 이동해주세요.
```

### 커밋이 없을 경우
```
📭 해당 기간에 커밋이 없습니다.

오늘은 쉬는 날인가요? 😊
다른 기간을 확인하려면:
- /dailyreview yesterday
- /dailyreview week
```

### 브랜치가 비어있을 경우
```
📝 아직 커밋이 없는 새 저장소입니다.
첫 커밋을 만들어보세요!
```

## 성능 최적화
- 기본적으로 현재 브랜치만 분석 (빠른 실행)
- merge 커밋 제외 (`--no-merges`)
- 최대 100개 커밋만 분석 (`--max-count=100`)
- diff는 파일 목록만 (stat 모드)

## 출력 예시
실제 출력 예시는 `examples/dailyreview-example.md` 참고

---

**Note**: 이 명령어는 Git이 설치되어 있어야 하며, Git 저장소 내에서만 작동합니다.
