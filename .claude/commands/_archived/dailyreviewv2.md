---
allowed-tools: [Bash, Task]
description: "오늘의 Git 커밋 간결 요약 (v2 최적화 버전)"
---

# /dailyreviewv2 - Daily Review (Optimized)

당신은 Git 커밋을 **간결하고 빠르게** 요약하는 전문가입니다.

## 핵심 원칙
- **속도 우선**: 통합 Git 명령으로 1회 데이터 수집
- **간결성**: 기본 10줄 이내 출력
- **점진적 공개**: 플래그로 상세도 조절

## 사용법
```bash
/dailyreviewv2              # 간결한 요약 (기본)
/dailyreviewv2 --brief      # 초간결 (3줄)
/dailyreviewv2 --full       # v1 수준 상세 정보
/dailyreviewv2 yesterday    # 어제 작업
/dailyreviewv2 week         # 최근 7일
```

## 실행 단계

### 1. Git 저장소 확인 및 시간 범위 결정
```bash
# 저장소 확인
git rev-parse --is-inside-work-tree 2>/dev/null || { echo "❌ Not a git repo"; exit 1; }

# 시간 범위 결정 ($ARGUMENTS 기반)
# 기본/today: --since="today 00:00"
# yesterday: --since="yesterday 00:00" --until="yesterday 23:59"
# week: --since="7 days ago"
```

### 2. 통합 데이터 수집 (1회 Bash 호출)
**핵심**: 모든 정보를 한 번의 명령으로 수집

```bash
# 통합 명령어
git log --since="[시간범위]" \
  --pretty=format:"COMMIT:%H|%ai|%s|%an" \
  --stat \
  --no-merges \
  --max-count=20
```

이 명령어로 다음을 동시에 얻습니다:
- 커밋 해시, 날짜, 메시지, 작성자
- 변경 파일 통계 (insertions, deletions)

### 3. 출력 모드 선택

#### A) 기본 모드 (플래그 없음)
```markdown
# 📅 Daily Review - [날짜]

**[N]개 커밋 | [M]개 파일 | +[X]줄 -[Y]줄**

[HH:MM] [커밋 메시지] ([주요 변경 디렉토리])
[HH:MM] [커밋 메시지] ([주요 변경 디렉토리])

💡 주요 작업: [1-2줄 요약]
```

**예시**:
```markdown
# 📅 Daily Review - 2025.11.09

**2개 커밋 | 7개 파일 | +755줄**

18:07 feat: Add basic authentication module (src/auth)
18:07 feat: Add custom Claude Code commands (.claude/commands)

💡 주요 작업: 인증 모듈 추가, 커스텀 명령어 개발
```

#### B) --brief 모드
```markdown
📅 [날짜] | [N] commits | +[X] lines
✓ [핵심 작업 1줄 요약]
```

**예시**:
```markdown
📅 2025.11.09 | 2 commits | +755 lines
✓ 인증 모듈, Claude 명령어 추가
```

#### C) --full 모드
v1과 동일한 상세 출력 생성:
- 커밋 타임라인 (파일별 상세)
- 작업 패턴 분석
- 파일별 변경 빈도
- Own It 포트폴리오 연동 안내

### 4. 에러 처리

**Git 저장소 아님**:
```
❌ Not a git repo
💡 Run this command in a git repository
```

**커밋 없음**:
```
📭 No commits found for [기간]

Try: /dailyreviewv2 yesterday
     /dailyreviewv2 week
```

## 구현 가이드

### 핵심 전략
1. **Bash 명령 통합**: git log + --stat으로 1회 호출
2. **파싱 효율화**: 출력을 파싱하여 필요한 정보만 추출
3. **조건부 상세화**: 플래그에 따라 출력 수준 조절

### Git 명령어 안전성
```bash
# 방어적 코딩 패턴
COMMITS=$(git log --since="today 00:00" --pretty=format:"%H|%ai|%s|%an" --no-merges 2>/dev/null)

if [ -z "$COMMITS" ]; then
  echo "📭 No commits today"
  exit 0
fi

# 통계 수집 (fallback 포함)
git diff --shortstat "$(echo "$COMMITS" | tail -1 | cut -d'|' -f1)~1...HEAD" 2>/dev/null || \
git log --since="today 00:00" --stat --pretty=format:"" --no-merges 2>/dev/null
```

### 성능 목표
- ✅ Accept 요청: 1-3회 (v1: 8-14회)
- ✅ 실행 시간: <8초 (v1: ~30초)
- ✅ 기본 출력: <15줄 (v1: ~50줄)

## 주의사항
- 기본 모드는 정보 손실 없이 간결하게
- --brief는 즉시 확인용 (상세 정보 생략)
- --full은 v1 호환성 유지
- Git 명령어 에러 핸들링 필수

---

**v2 개선점**: 80% 짧은 출력, 85% 적은 승인, 73% 빠른 실행
